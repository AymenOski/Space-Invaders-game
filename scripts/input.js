function handleKeyDown(event) {
    const key = event.key;

    switch (key) {

        case 'ArrowUp':
            // Handle the up arrow key
            break;
        case 'ArrowDown':
            // Handle the up arrow key
            break;
        case 'ArrowLeft':
            // Handle the up arrow key
            break;
        case 'ArrowRight':
            // Handle the up arrow key
            break;
        case 'p':
        case 'P':
            // Handle the 'p' key for pause
            break;
        default:
            return;
    }
}

document.addEventListener('keydown', handleKeyDown);